<div class="container">
  <div class="row">
    <div class="project-cards">
      <% @portofolios.each do |last_project| %>
        <%= render partial: "shared/last-projects", locals: {last_project: last_project} %>
      <% end %>
    </div>
  </div>
</div>
